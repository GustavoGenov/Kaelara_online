{
  "name": "kaelara-workspace",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "build": "npm --prefix frontend install && npm --prefix frontend run build"
  },
  "engines": {
    "node": ">=20"
  }
}
