name: Keep Kaelara Alive

on:
  schedule:
    - cron: "*/10 * * * *"
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Kaelara Render URL
        run: curl -s https://kaelara-online.onrender.com/api/chat > /dev/null || true
